Na aula de hoje, trabalharemos renomeando, excluindo, relocando e verificando logs de alteração

importante lemrbar a ordem

criar Pasta> VsCode > Abrir Pasta > Novo terminal

então entrar no git hub > novo repo > Nomear de algo

então instanciar, linkar e etc

criamos o projeto sobre o projeto txt, escrevemos, ctrl S

então no terminal fomo: git init > git add .\sobre projeto >
git branch -m master
git commit .\sobre projeto > -m "comentários"
git push -u origin master 

e isso linka com o github



então renomeamos fazendo o seguinte

git mv .\ (selecionar Sobre o projeto no TAB) '.\readme.txt'
